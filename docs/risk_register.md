# Risk Register

- If CosyVoice controllability is weak, demote affected samples to Silver or Bronze.
- If Emilia retrieval is noisy, allow manual curation metadata instead of blocking the pipeline.
- If modality penalty is high, mark the slice inconclusive rather than forcing a claim.
- If pragmatic intent stays unstable, reduce the label space before more generation.
