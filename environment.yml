name: baer-refqa
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - -e ".[dev]"
    - torch>=2.0
    - transformers>=4.40
    - openai-whisper>=20231117
    - tqdm>=4.66
    - python-dotenv>=1.0
